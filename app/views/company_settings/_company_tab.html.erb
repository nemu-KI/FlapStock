<div class="space-y-8">
  <!-- 現在の設定表示 -->
  <div class="bg-gray-50 rounded-lg p-6">
    <h2 class="text-lg font-semibold text-gray-900 mb-4 flex items-center">
      <svg class="w-5 h-5 mr-2 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
      </svg>
      現在の設定
    </h2>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <!-- 基本情報 -->
      <div class="space-y-4">
        <h3 class="text-md font-semibold text-gray-800 border-b border-gray-300 pb-2">基本情報</h3>

        <div>
          <label class="block text-sm font-medium text-gray-600">会社名</label>
          <p class="mt-1 text-sm text-gray-900 font-medium"><%= @company.name %></p>
        </div>

        <div>
          <label class="block text-sm font-medium text-gray-600">メールアドレス</label>
          <p class="mt-1 text-sm text-gray-900 font-medium"><%= @company.email %></p>
        </div>

        <div>
          <label class="block text-sm font-medium text-gray-600">電話番号</label>
          <p class="mt-1 text-sm text-gray-900 font-medium"><%= @company.phone.present? ? @company.phone : '未設定' %></p>
        </div>
      </div>

      <!-- 連絡先情報 -->
      <div class="space-y-4">
        <h3 class="text-md font-semibold text-gray-800 border-b border-gray-300 pb-2">連絡先情報</h3>

        <div>
          <label class="block text-sm font-medium text-gray-600">住所</label>
          <p class="mt-1 text-sm text-gray-900 font-medium"><%= @company.address.present? ? @company.address : '未設定' %></p>
        </div>

        <div>
          <label class="block text-sm font-medium text-gray-600">タイムゾーン</label>
          <p class="mt-1 text-sm text-gray-900 font-medium">
            <%= @company.timezone.present? ? ActiveSupport::TimeZone[@company.timezone]&.to_s : 'Asia/Tokyo' %>
          </p>
        </div>
      </div>
    </div>
  </div>

  <!-- 編集フォーム -->
  <div class="bg-white border border-gray-200 rounded-lg p-6">
    <h2 class="text-lg font-semibold text-gray-900 mb-4 flex items-center">
      <svg class="w-5 h-5 mr-2 text-blue-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
      </svg>
      設定を変更
    </h2>

    <%= form_with model: @company, url: settings_path, local: true, class: "space-y-6" do |form| %>
      <% if @company.errors.any? %>
        <div class="rounded-md bg-red-50 p-4">
          <div class="flex">
            <div class="ml-3">
              <h4 class="text-sm font-medium text-red-800">
                <%= pluralize(@company.errors.count, "個のエラー") %>が発生しました：
              </h4>
              <div class="mt-2 text-sm text-red-700">
                <ul class="list-disc pl-5 space-y-1">
                  <% @company.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                  <% end %>
                </ul>
              </div>
            </div>
          </div>
        </div>
      <% end %>

      <div class="bg-gray-50 rounded-lg p-4">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          <!-- 基本情報フォーム -->
          <div class="space-y-4">
            <h4 class="text-md font-semibold text-gray-800 border-b border-gray-300 pb-2">基本情報</h4>

            <div>
              <%= form.label :name, "会社名", class: "block text-sm font-medium text-gray-700" %>
              <%= form.text_field :name, class: "mt-1 block w-full border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm" %>
            </div>

            <div>
              <%= form.label :email, "メールアドレス", class: "block text-sm font-medium text-gray-700" %>
              <%= form.email_field :email, class: "mt-1 block w-full border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm" %>
            </div>

            <div>
              <%= form.label :phone, "電話番号", class: "block text-sm font-medium text-gray-700" %>
              <%= form.text_field :phone, placeholder: "例: 03-1234-5678", class: "mt-1 block w-full border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm" %>
              <p class="mt-1 text-xs text-gray-500">数字、ハイフン、括弧、プラス記号が使用できます</p>
            </div>
          </div>

          <!-- 連絡先情報フォーム -->
          <div class="space-y-4">
            <h4 class="text-md font-semibold text-gray-800 border-b border-gray-300 pb-2">連絡先情報</h4>

            <div>
              <%= form.label :address, "住所", class: "block text-sm font-medium text-gray-700" %>
              <%= form.text_area :address, rows: 3, placeholder: "例: 東京都渋谷区...", class: "mt-1 block w-full border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm" %>
            </div>

            <div>
              <%= form.label :timezone, "タイムゾーン", class: "block text-sm font-medium text-gray-700" %>
              <%= form.select :timezone,
                  options_from_collection_for_select(ActiveSupport::TimeZone.all, :name, :to_s, @company.timezone),
                  { prompt: "タイムゾーンを選択してください" },
                  { class: "mt-1 block w-full border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm" } %>
            </div>
          </div>
        </div>
      </div>

      <!-- ボタン -->
      <div class="flex justify-end space-x-3 pt-6 border-t border-gray-200">
        <%= form.submit "更新", class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
      </div>
    <% end %>
  </div>

  <!-- 設定の説明 -->
  <div class="mt-8 p-4 bg-blue-50 rounded-lg">
    <h4 class="text-sm font-medium text-blue-900 mb-2">設定について</h4>
    <p class="text-sm text-blue-800">
      これらの情報は発注メールの署名や通知機能で使用されます。正確な情報を設定することで、より効果的な在庫管理が可能になります。
    </p>
  </div>
</div>
