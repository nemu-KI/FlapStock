<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>パスワードリセット - FlapStock</title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      line-height: 1.5;
      color: #333;
      max-width: 500px;
      margin: 0 auto;
      padding: 20px;
    }
    .container {
      background-color: white;
      padding: 25px;
    }
    .header {
      text-align: left;
      margin-bottom: 25px;
    }
    .logo {
      font-size: 18px;
      font-weight: bold;
      color: #3b82f6;
      margin-bottom: 8px;
    }
    .title {
      font-size: 16px;
      color: #374151;
      text-align: left;
    }
    .content {
      margin-bottom: 25px;
    }
    .button {
      display: inline-block;
      background-color: #3b82f6;
      color: white;
      padding: 10px 20px;
      text-decoration: none;
      border-radius: 4px;
      font-weight: bold;
      margin: 15px 0;
    }
    .button:hover {
      background-color: #2563eb;
    }
    .footer {
      margin-top: 25px;
      padding-top: 15px;
      border-top: 1px solid #e5e7eb;
      font-size: 13px;
      color: #6b7280;
    }
    .warning {
      margin: 15px 0;
      color: #92400e;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <div class="logo">FlapStock</div>
      <div class="title">パスワードリセット</div>
    </div>

    <div class="content">
      <p>こんにちは、<%= @resource.email %>様</p>

      <p>FlapStockのパスワードリセットのリクエストを受け付けました。</p>

      <p>パスワードを変更するには、以下のリンクをクリックしてください：</p>

      <div style="text-align: left; margin: 20px 0;">
        <a href="<%= edit_password_url(@resource, reset_password_token: @token, host: ENV['APP_HOST'] || 'flapstock.onrender.com') %>" style="color: #3b82f6; text-decoration: underline; font-weight: bold; font-size: 16px;">
          <%= edit_password_url(@resource, reset_password_token: @token, host: ENV['APP_HOST'] || 'flapstock.onrender.com') %>
        </a>
      </div>

      <div class="warning">
        <strong>注意：</strong>
        <ul style="margin: 10px 0 0 20px; padding: 0;">
          <li>このリンクは24時間後に無効になります</li>
          <li>このリクエストを送信していない場合は、このメールを無視してください</li>
          <li>パスワードは変更されません</li>
        </ul>
      </div>


    </div>

    <div class="footer">
      <p>このメールは自動送信されています。返信はできません。</p>
      <p>FlapStock</p>
    </div>
  </div>
</body>
</html>
