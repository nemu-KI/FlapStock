<li class="page-item list-none <%= 'active' if page.current? %>">
  <%= link_to page, url, {remote: remote, rel: page.rel, class: "page-link px-3 py-2 text-sm font-medium rounded-md #{page.current? ? 'bg-blue-600 text-white' : 'text-gray-500 bg-white border border-gray-300 hover:bg-gray-50 hover:text-gray-700'}"} %>
</li>
